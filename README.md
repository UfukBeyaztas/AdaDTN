Type the following to install the AdaDTN package:


install.packages("remotes")
remotes::install_github("UfukBeyaztas/AdaDTN")


A detailed user manual is available in the GitHub repository. The manual describes the main functions, arguments, return values, and recommended workflows for fitting, tuning, predicting, and interpreting functional response models.
